<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>Books For Life</title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <div class="jumbotron text-center">
      <h1>Welcome to Books for Life App.</h1>
      <h3>Your gate to grate books</h3>
    </div>
    <div class="table-responsive">
      <table class="table table-hover">
        <tr>
          <th>Title</th>
          <th>Author</th>
          <th>Cover Image</th>
          <th>Edit</th>
          <th>Delete</th>
        </tr>
        <% if (books){books.forEach(book => { %>
        <tr>
          <td><%= book.title %></td>
          <td><%= book.author %></td>
          <td>
            <img src="/uploads/<%= book.coverImage %>" style="width: 40%" />
          </td>
          <td><a href="/books/<%= book._id %>/show">Show</a></td>
          <td><a href="/books/<%= book._id %>/delete">Delete</a></td>
        </tr>
        <% })}else{%>
        <div>No results were found</div>
        <%} %>
      </table>
    </div>
  </body>
</html>
